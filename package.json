{
  "name": "federicocunico-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run media && vite",
    "media": "node scripts/build-media.mjs",
    "generate:cv": "node scripts/generate-cv.mjs",
    "build": "npm run media && npm run generate:cv && vite build",
    "preview": "vite preview",
    "test": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "pdfmake": "^0.2.20",
    "vue": "^3.5.13",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "sharp": "^0.35.4",
    "vite": "^6.0.7"
  }
}
